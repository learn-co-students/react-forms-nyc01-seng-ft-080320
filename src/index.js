import React from 'react';
import ReactDOM from 'react-dom';
import Form from './components/Form'
import ParentComponent from './ParentComponenet';

ReactDOM.render(
  <div>
    <ParentComponent />
  </div>,
  document.getElementById('root')
);